apiVersion: v1
kind: Secret
metadata:
  name: anton-secrets
  namespace: agents
type: Opaque
data:
  # Base64-encoded values — replace before applying
  # echo -n "your-key" | base64
  anthropic-api-key: ""
  openai-api-key: ""
  github-token: ""
  webhook-secret: ""
